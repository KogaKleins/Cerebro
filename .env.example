# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                    🧠 CÉREBRO - Variáveis de Ambiente                        ║
# ║                                                                              ║
# ║  Copie este arquivo para .env e preencha os valores adequados               ║
# ║  NUNCA commite o arquivo .env no repositório!                               ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ SERVIDOR                                                                      │
# └──────────────────────────────────────────────────────────────────────────────┘

# Porta do servidor (padrão: 3000)
PORT=3000

# Ambiente de execução: development | production | test
NODE_ENV=development

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ BANCO DE DADOS                                                                │
# └──────────────────────────────────────────────────────────────────────────────┘

# URL de conexão PostgreSQL
# Formato: postgresql://[usuario]:[senha]@[host]:[porta]/[database]?schema=[schema]
#
# Exemplos:
#   Local:      postgresql://postgres:minhasenha@localhost:5432/cerebro?schema=public
#   Supabase:   postgresql://postgres:[SENHA]@db.[ID].supabase.co:5432/postgres?schema=public
#   Railway:    postgresql://postgres:[SENHA]@[HOST]:5432/railway?schema=public
#   Render:     postgresql://[USER]:[SENHA]@[HOST]/[DATABASE]
#
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cerebro?schema=public"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ AUTENTICAÇÃO & SEGURANÇA                                                      │
# └──────────────────────────────────────────────────────────────────────────────┘

# Chave secreta para tokens JWT
# ⚠️ IMPORTANTE: Em produção, use uma chave longa e aleatória!
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_secreta_muito_segura_mude_em_producao_123456789

# Tempo de expiração do token JWT
# Formatos: 60, "2 days", "10h", "7d", "1y"
JWT_EXPIRES_IN=24h

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ CORS - Origens Permitidas                                                     │
# └──────────────────────────────────────────────────────────────────────────────┘

# Lista de origens permitidas (separadas por vírgula)
# Em desenvolvimento, adicione localhost e IPs da rede local
# Em produção, adicione apenas domínios autorizados
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ RATE LIMITING - Proteção contra abuso                                         │
# └──────────────────────────────────────────────────────────────────────────────┘

# Janela de tempo para rate limiting (em milissegundos)
# Padrão: 900000 (15 minutos)
RATE_LIMIT_WINDOW_MS=900000

# Máximo de requisições por janela de tempo
# Padrão: 1000 (desenvolvimento), 100 (produção)
RATE_LIMIT_MAX_REQUESTS=1000

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ CACHE (Opcional - Redis)                                                      │
# └──────────────────────────────────────────────────────────────────────────────┘

# URL do Redis para cache (opcional)
# Se não configurado, o sistema funciona sem cache
# Formato: redis://[usuario]:[senha]@[host]:[porta]
# REDIS_URL=redis://localhost:6379

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ MONITORAMENTO (Opcional - Sentry)                                             │
# └──────────────────────────────────────────────────────────────────────────────┘

# DSN do Sentry para monitoramento de erros (opcional)
# Obtenha em: https://sentry.io/
# SENTRY_DSN=https://xxxxx@xxx.ingest.sentry.io/xxxxx

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ USUÁRIOS INICIAIS (Opcional - Para migração)                                  │
# └──────────────────────────────────────────────────────────────────────────────┘

# Usuários podem ser definidos aqui para migração inicial
# Após a migração, eles ficam no banco de dados
# Formato: username:hashedPassword:name:role:avatar:setor
#
# Para gerar hash de senha:
#   node -e "const bcrypt = require('bcrypt'); bcrypt.hash('suasenha', 10).then(h => console.log(h))"
#
# USER_ADMIN=admin:$2b$10$...:Administrador:admin:👑:Geral
# USER_MEMBER=membro:$2b$10$...:Membro:member:🧑:Equipe

# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                           FIM DA CONFIGURAÇÃO                                 ║
# ║                                                                              ║
# ║  Após configurar, execute:                                                   ║
# ║    npm install                                                               ║
# ║    npx prisma generate                                                       ║
# ║    npx prisma migrate dev                                                    ║
# ║    npm run dev                                                               ║
# ╚══════════════════════════════════════════════════════════════════════════════╝
